---
export interface Props {
  articles: any[];
  total?: number;
  page?: number;
}

const { articles, total = 0, page = 1 } = Astro.props;
---
<div class="article-table-wrapper">
  <p class="table-count">{total} article{total !== 1 ? 's' : ''}</p>
</div>
