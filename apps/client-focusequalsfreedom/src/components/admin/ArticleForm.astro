---
export interface Props {
  article?: any;
}
const { article } = Astro.props;
---
<!-- ArticleForm is implemented inline in the editor pages -->
<div class="article-form-placeholder">
  {article ? `Editing: ${article.title}` : 'New Article Form'}
</div>
