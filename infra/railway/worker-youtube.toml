[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build:workers"

[deploy]
startCommand = "npm run start:worker:youtube"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
